#include "main.h"
#include "2-strlen.c"
/**
 * rev_string - function to reverse a string.
 * @s: char parameter.
 *
 */
void rev_string(char *s)
{
	int len;

	len = _strlen(s);
	for (i = len - 1; i >= 0; i--)
	{

	}
}
