#include <stdio.h>
/**
 * main - function of command line arguments.
 * @argc: number of comand line arguments.
 * @argv: array of string values
 * Return: 0 always.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
